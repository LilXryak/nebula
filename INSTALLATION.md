# 🌌 Nebula - Инструкция по установке

## Автоматическая установка (Рекомендуется)

Для автоматической установки Nebula используйте скрипт `deploy.sh`:

```bash
git clone https://github.com/LilXryak/nebula.git
cd nebula
sudo bash deploy.sh
```

Скрипт автоматически выполнит все необходимые шаги установки.

## Важные замечания перед установкой

### ✅ Директории уже переименованы

Все директории уже переименованы:
- `backend/videocall_app` → `backend/nebula_app` ✓
- `videocall-frontend` → `nebula-frontend` ✓

Проект готов к установке!

## Системные требования

- **ОС**: Ubuntu 20.04+ (или аналогичная Linux-система)
- **RAM**: Минимум 2GB, рекомендуется 4GB+
- **CPU**: 2+ ядра
- **Диск**: 20GB+ свободного места
- **Домен**: Собственный домен с настроенной DNS записью (A запись на IP сервера)

## Процесс установки

1. **Клонирование репозитория**
   ```bash
   git clone https://github.com/LilXryak/nebula.git
   cd nebula
   ```

2. **Запуск автоматического установщика**
   ```bash
   sudo bash deploy.sh
   ```

4. **Следование инструкциям установщика**
   - Введите ваш домен
   - Введите данные администратора
   - Дождитесь завершения установки

## После установки

После успешной установки Nebula будет доступен по вашему домену:
- **Frontend**: https://yourdomain.com
- **API**: https://yourdomain.com/api
- **Admin панель**: https://yourdomain.com/admin

## Управление

```bash
# Переход в директорию проекта
cd /opt/nebula  # или /home/username/nebula

# Просмотр логов
docker compose logs -f

# Остановка сервисов
docker compose down

# Запуск сервисов
docker compose up -d

# Перезапуск сервисов
docker compose restart
```

## Обновление

Для обновления Nebula:

```bash
cd /opt/nebula  # или /home/username/nebula
git pull origin main
docker compose down
docker compose build
docker compose up -d
docker compose exec backend python manage.py migrate
```

## Решение проблем

### SSL сертификат не выдается

Убедитесь, что:
- Домен указывает на IP сервера (A запись)
- Порты 80 и 443 открыты в файрволе
- DNS изменения успели распространиться

### WebSocket не работает

Проверьте:
- Файл `nginx.conf` содержит правильный домен
- Переменная `DOMAIN_NAME` в `.env` настроена правильно
- CORS настройки в `backend/nebula_app/settings.py` содержат ваш домен

### Контейнеры не запускаются

Проверьте логи:
```bash
docker compose logs
```

Убедитесь, что:
- Порты 80 и 443 не заняты другими приложениями
- Достаточно места на диске
- Достаточно оперативной памяти

## Поддержка

Если у вас возникли проблемы, проверьте:
- Логи приложения: `docker compose logs -f`
- Статус контейнеров: `docker compose ps`
- Конфигурационные файлы: `.env`, `nginx.conf`
