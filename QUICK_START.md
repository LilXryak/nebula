# 🚀 Быстрый старт Nebula

## Автоматическая установка

Для автоматической установки Nebula на Ubuntu сервер:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/LilXryak/nebula.git
cd nebula

# 2. Запустите установщик
sudo bash deploy.sh
```

Скрипт автоматически выполнит все необходимые шаги установки.

## Что нужно перед установкой

- ✅ Ubuntu 20.04+ сервер
- ✅ Собственный домен (например: example.com)
- ✅ DNS A запись домена на IP сервера

## После установки

- **Frontend**: https://yourdomain.com (пароль доступа: `admin123`)
- **API**: https://yourdomain.com/api
- **Django Admin**: https://yourdomain.com/admin (username: `admin`, password: `admin123`)
- **Пароль доступа к сайту**: `admin123` (можно изменить в админ-панели)

## Управление

```bash
# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d

# Исправление ошибок (если что-то не работает)
bash fix-errors.sh
```

## Решение проблем

Если возникают ошибки:
1. **Ошибка 500 в админке** - выполните: `bash fix-errors.sh`
2. **Не создается администратор** - выполните: `bash fix-errors.sh` или вручную: `docker compose exec backend python manage.py createsuperuser`
3. **Проблемы с БД** - скрипт `fix-errors.sh` автоматически пересоздаст БД при необходимости
4. **Ошибка 521 (Web server is down)** - проверьте что все контейнеры запущены: `docker compose ps` и если nginx/frontend не запущены: `docker compose up -d`

Готово! Наслаждайтесь Nebula! 🌌
